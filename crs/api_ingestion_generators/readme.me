Scarichiamo una pagina intera,
yieldiamo record singoli
processiamo record singoli e li mettiamo in un batch
quando il len batch == x allora carichiamo in clickhouse
puliamo il batch, prendiamo altre pagine 
se non ci sono altri dati carichiamo l'ultimo batch e terminiamo il programma